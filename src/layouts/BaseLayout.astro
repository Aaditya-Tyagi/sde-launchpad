---
import '../styles/global.css';
const {
  title = 'SDE-2 Launchpad',
  desc = 'Your interactive, offline interview-prep platform.',
  active = '',
  program = 'sde2',
} = Astro.props;
const base = import.meta.env.BASE_URL.replace(/\/$/, '');

const NAVS = {
  sde2: {
    brand: 'SDE-2 Launchpad',
    logo: 'S2',
    home: '/sde2',
    links: [
      { href: '/sde2', label: 'Today', key: 'home' },
      { href: '/plan', label: 'Plan', key: 'plan' },
      { href: '/tracks', label: 'Learn', key: 'tracks' },
      { href: '/game', label: '🎮 DSA Quest', key: 'game' },
      { href: '/practice/code', label: 'Code Lab', key: 'code' },
      { href: '/practice/quiz', label: 'Output Quiz', key: 'quiz' },
      { href: '/practice/flashcards', label: 'Flashcards', key: 'flash' },
      { href: '/practice/mock', label: 'Mock', key: 'mock' },
      { href: '/practice/stories', label: 'STAR Stories', key: 'stories' },
      { href: '/practice/design', label: 'SD Canvas', key: 'design' },
      { href: '/mistakes', label: 'Mistakes Log', key: 'mistakes' },
    ],
    footer: [
      { href: '/tracks', label: 'Curriculum' },
      { href: '/game', label: 'DSA Quest' },
      { href: '/practice/design', label: 'SD Canvas' },
      { href: '/prep', label: 'Company Prep' },
      { href: '/cheatsheets', label: 'Cheat Sheets' },
      { href: '/diagnostic', label: 'Diagnostic' },
      { href: '/mistakes', label: 'Mistakes' },
    ],
  },
  qa: {
    brand: 'QA · Automation Launchpad',
    logo: 'QA',
    home: '/qa',
    links: [
      { href: '/qa', label: 'Today', key: 'home' },
      { href: '/qa/plan', label: '12-Week Plan', key: 'plan' },
      { href: '/qa/tracks', label: 'Learn', key: 'tracks' },
      { href: '/lessons/qa-web/playwright-core', label: 'Playwright', key: 'pw' },
      { href: '/lessons/qa-web/selenium-core', label: 'Selenium', key: 'sel' },
      { href: '/lessons/qa-api/http-foundations', label: 'API', key: 'api' },
      { href: '/lessons/qa-ai/ai-in-testing-2026', label: '🤖 AI Testing', key: 'ai' },
      { href: '/lessons/qa-cheatsheets/cheatsheets', label: 'Cheat Sheets', key: 'cheat' },
      { href: '/mistakes', label: 'Mistakes Log', key: 'mistakes' },
    ],
    footer: [
      { href: '/qa/tracks', label: 'Curriculum' },
      { href: '/qa/plan', label: '12-Week Plan' },
      { href: '/lessons/qa-web/playwright-core', label: 'Playwright' },
      { href: '/lessons/qa-api/http-foundations', label: 'API Testing' },
      { href: '/lessons/qa-cheatsheets/cheatsheets', label: 'Cheat Sheets' },
      { href: '/lessons/qa-career/certifications-and-roadmap', label: 'Certifications' },
      { href: '/mistakes', label: 'Mistakes' },
    ],
  },
};
const isHome = program === 'home';
const nav = NAVS[program] || NAVS.sde2;
const brandName = isHome ? 'Interview Launchpad' : nav.brand;
const brandLogo = isHome ? 'IL' : nav.logo;
const brandHref = isHome ? '/' : nav.home;
const links = isHome ? [] : nav.links;
const footerLinks = isHome ? [] : nav.footer;
---
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>{title}</title>
  <meta name="description" content={desc} />
  <meta name="theme-color" content="#0b1326" />
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <!-- Load fonts without blocking first paint: fetch the stylesheet as
       non-render-blocking (media=print), then flip it on once loaded. Text
       renders immediately in the fallback and swaps in via display=swap. -->
  <link
    rel="stylesheet"
    href="https://fonts.googleapis.com/css2?family=Hanken+Grotesk:wght@400;500;600;700;800&family=JetBrains+Mono:wght@400;500;700&display=swap"
    media="print"
    onload="this.media='all'"
  />
  <noscript>
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Hanken+Grotesk:wght@400;500;600;700;800&family=JetBrains+Mono:wght@400;500;700&display=swap" />
  </noscript>
  <link rel="manifest" href={`${base}/manifest.webmanifest`} />
  <link rel="icon" href={`${base}/favicon.svg`} type="image/svg+xml" />
  <script is:inline src={`${base}/store.js`}></script>
  <script is:inline src={`${base}/anim.js`} defer></script>
  <slot name="head" />
</head>
<body>
  <div class="aurora-bg" aria-hidden="true"></div>
  <nav class="topnav">
    <div class="inner">
      <a class="brand" href={`${base}${brandHref}`}>
        <span class="logo">{brandLogo}</span>
        <span>{brandName}</span>
      </a>
      <div class="navlinks" id="navlinks">
        {links.map((l) => (
          <a href={`${base}${l.href}`} class={active === l.key ? 'active' : ''}>{l.label}</a>
        ))}
        {!isHome && <a href={`${base}/`} class="nav-switch" title="Switch path">⇄ Paths</a>}
      </div>
      <div class="nav-right">
        <button class="cmdk" id="cmdkBtn" title="Search lessons">
          <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2"><circle cx="11" cy="11" r="7"/><path d="M21 21l-4.3-4.3" stroke-linecap="round"/></svg>
          <span class="cmdk-text">Search <kbd>⌘K</kbd></span></button>
        <span class="streakpill" id="streakPill" title="Daily streak">
          <svg width="14" height="14" viewBox="0 0 24 24" fill="currentColor"><path d="M13 2c.6 3.6-1.6 5.2-3 7-1 1.4-.6 3.1 1 3.1 1.5 0 2.2-1.4 1.9-2.8 2.4 1.7 3.4 4.5 2.1 7.2-1 2-2.9 3-5 3-3 0-5.4-2.3-5.4-5.5 0-2.8 1.8-4.8 3.5-6.6C9.6 6.5 11.4 4.9 13 2z"/></svg>
          <span id="streakNum">0</span></span>
        <span class="xppill" id="xpPill" title="Level / XP">
          <svg width="14" height="14" viewBox="0 0 24 24" fill="currentColor"><path d="M12 2l2.9 6.3 6.9.6-5.2 4.6 1.6 6.8L12 17.3 5.8 20.9l1.6-6.8L2.2 8.9l6.9-.6z"/></svg>
          <span id="lvlNum">1</span></span>
        <span class="nav-avatar" aria-hidden="true">S</span>
      </div>
      <button class="nav-burger" id="navBurger" aria-label="Menu" aria-expanded="false">
        <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2"><path d="M3 6h18M3 12h18M3 18h18" stroke-linecap="round"/></svg>
      </button>
    </div>
  </nav>

  <main>
    <slot />
  </main>

  <footer class="site">
    <div class="wrap site-foot" style="padding:0">
      <div>
        <div style="font-size:20px;font-weight:700;color:var(--text)">{brandName}</div>
        <p style="margin-top:6px;max-width:460px">Everything is stored locally in your browser; nothing leaves this device. Precision practice for technical mastery.</p>
        <button class="btn hide" id="installBtn" style="margin-top:12px">📲 Install as an app</button>
      </div>
      <nav class="site-foot-links">
        {footerLinks.map((l) => <a href={`${base}${l.href}`}>{l.label}</a>)}
        <a href={`${base}/`}>⇄ Switch path</a>
      </nav>
    </div>
  </footer>

  <!-- Command palette -->
  <div class="cmdk-overlay" id="cmdkOverlay">
    <div class="cmdk-box">
      <input type="text" id="cmdkInput" placeholder="Search topics, patterns, problems…" autocomplete="off" />
      <div class="cmdk-results" id="cmdkResults"></div>
    </div>
  </div>

  <!-- App-shell behaviour (pills, nav, command palette, PWA, SW). Extracted to
       an external deferred file so it ships/parses once and is cached across
       pages, instead of being inlined into every built HTML document. -->
  <script is:inline src={`${base}/app.js`} defer></script>
</body>
</html>
